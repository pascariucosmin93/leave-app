# Scaling

- Backend HPA is enabled through the GitOps chart.
- Frontend HPA is enabled through the GitOps chart.
- Default targets remain CPU-based and can be tuned in `gitops-leave-app/k8s/chart/values.yaml`.

